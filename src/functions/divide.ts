/**
 * Divide a number.
 * @param number Number to divide
 * @param by Number to divide `number` by
 */
function divide(number: number, by: number): number {
    return number / by;
}

export { divide };
